@model IEnumerable<TeamTracker.Models.Project>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.project_name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.project_name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.project_code">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.project_code">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.project_code">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
